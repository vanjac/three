Python 3.4

Pip packages:

PyOpenGL
PyOpenGL_accelerate
pyautogui
  For Linux this requires the pillow pip package, along with the Linux packages scrot, python3-tk, python3-dev, and python3-xlib.
  OS X / macOS may require other pip packages; look it up.
  Windows shouldn't require anything.